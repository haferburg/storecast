#pragma once

#include <cstdint>

namespace storecast
{

typedef int32_t i32;
typedef float f32;

#define for3(I) for(auto I=0; I<3; ++I)
#define for4(I) for(auto I=0; I<4; ++I)

} // namespace storecast
